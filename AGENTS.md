# xiaoxiao-pet 项目协作规则

本文件仅适用于 `/Users/admin/xiaoxiao-pet` 项目及其子目录。

## 当前平台范围

- 本项目是独立的 App 级桌面宠物，不是 Codex 级别的 Pet，也不以 Codex Pet 的精灵表、动画行或打包规范作为项目标准。
- 在 macOS 版本完成前，只处理、预览、构建和发布 macOS 版本。
- 不主动修改、同步、构建或发布 Windows 版本；除非用户之后明确要求。

## hatch-pet 与 ImageGen 调用边界

- `hatch-pet` 和 ImageGen 调用耗时较长，默认禁止调用；不能因为项目涉及桌宠、动作、图片或 GIF 就自动使用。
- 日常动作调试、现有精灵表切帧、帧序调整、速度调整、缩放、位移、旋转、镜像、GIF 装配和预览导出，都应优先使用项目现有源码、素材及本地确定性工具完成。
- 不得使用 `hatch-pet` 检查、生成、修复或打包本项目的 App 桌宠动作。本项目不需要遵循 Codex Pet v2、8×11 精灵表、9 行动画或 16 个方向等 Codex Pet 专用规范。
- 只有用户明确要求创建、转换或打包 Codex Pet 时，才允许调用 `hatch-pet`。
- 只有用户明确要求使用 AI 新生成或编辑位图素材，或者现有素材和本地工具确实无法完成目标时，才考虑调用 ImageGen。
- 因现有素材和本地工具无法完成而需要 ImageGen 时，必须先说明必要性和预计用途，并取得用户明确同意后才能调用。
- 能通过裁切、合成、透明背景处理、尺寸调整、帧提取或代码绘制完成的任务，不得调用 ImageGen。

## 动作修改与 GIF 审核

- 凡是会改变动作单次播放视觉效果的修改，必须先制作对应 GIF 交给用户审核。
- 一个动作对应一个 GIF；多个动作必须分别命名并分别导出，不能合并在同一个 GIF 中。
- GIF 必须完整展示一次动作，从开始状态播放到结束并回到稳定状态。
- GIF 必须保持应用内的实际播放速度，不得通过后期加速、减速或裁掉问题帧美化效果。
- 审核 GIF 统一导出到 `/Users/admin/Desktop/xiaoxiao-gif`，不得导出到项目目录或混入正式发布产物。
- 为制作 GIF，可以修改源码和素材，但前提是修改可完整回撤。修改前必须保留原文件副本，并明确记录本次修改过的文件。
- 临时预览构建必须放在独立临时目录，不得覆盖正式发布目录或正式安装包。
- 只有用户明确回复“通过”“可以发布”或其他含义同样明确的发布授权，才视为审核通过。“看起来不错”、提出后续需求或没有反对都不视为通过。
- 用户的通过只覆盖当次 GIF 中展示的效果。通过后如果再次修改动画帧、帧序、速度、缩放、位移、旋转、镜像或循环方式，原审批自动失效，必须重新导出 GIF 并审核。
- 一次需求包含多个动作时，可以逐个通过；只有已明确通过的动作可以进入发布，未通过的动作不得夹带发布。

## 不需要 GIF 的修改

- 与动作单次动画效果无关的操作可以直接修改，不需要等待 GIF 审核，例如菜单文字、说明文档、更新机制、动作触发条件、状态切换逻辑，以及没有视觉变化的问题修复。
- 如果逻辑修改同时改变了动作的实际视觉表现，仍然必须走 GIF 审核流程。

## 审核前禁止发布

动作效果未明确通过前，禁止：

- 修改正式版本号或构建号。
- 覆盖 `outputs` 中的正式产物。
- 生成或替换正式更新 ZIP、正式 `.app`、`update.json` 等发布文件。
- 运行 `发布本地更新.command` 或其他正式发布脚本。
- 向用户的本地更新目录写入更新包。

审核阶段完成时，必须明确告诉用户：`当前仅供审核，尚未发布，无需更新或下载。`

## 发布与安装提示

- 动作修改审核通过后，才可以进行版本调整、正式构建和更新发布。
- 与动作效果无关的修改可以直接完成并发布。
- 每次正式发布完成后，必须根据实际安装方式明确使用以下其中一句：
  - `安装方式：右键选择「更新」即可`
  - `安装方式：需要下载新版`
- 只要用户当前安装的 macOS 版本能够通过右键“更新”正确安装本次产物，就提示更新即可。
- 只有更新器无法覆盖、应用身份或安装方式变化等必须重新安装的情况，才提示需要下载新版；不能仅凭版本号猜测。
- 没有实际生成并放置可安装更新产物时，不得声称用户已经可以更新。

## 版本号规则

- 普通小改动只增加 build，不提升 `1.0.x` 展示版本号。
- 较明显的功能更新才增加 `1.0.x`，同时增加 build。
- 是否属于“较明显的功能更新”存在疑义时，发布前先向用户确认，不擅自提升展示版本号。

## 旧版本发布产物清理

- 正式发布完成后，自动检查旧版发布产物，始终保留最近 3 次正式发布的产物。
- 每次发布以“展示版本号 + build”共同识别，并主要按 build 判断先后。即使多个普通小改动都使用 `1.0.5`，也必须按各自 build 分别计为一次发布。
- 例如最近发布的是 `1.0.5 (build 8)`、`1.0.5 (build 7)`、`1.0.5 (build 6)`，则保留这 3 次发布，可清理 build 5 及更早的可再生成发布产物。展示版本号提升后，仍按实际发布顺序和 build 保留最近 3 次。
- 清理范围只包括已经确认属于旧版本、且可以重新生成的安装包、应用包、更新清单和对应的临时构建资源。
- 删除前必须先解析并列出精确目标，确认版本归属；不得使用未解析变量、宽泛通配符或递归删除项目根目录。
- 不得自动删除源码、原始动作素材、当前版本资源、用户数据、安装备份或无法确认用途的文件。
- 如果现有命名无法可靠判断版本归属，则暂停自动清理并向用户确认，不能猜测删除。
- 如果发布流程本来就使用固定文件名覆盖上一版、没有保存多个历史包，则不得为了执行清理而删除当前文件，也不需要虚构清理结果。
- 完成清理后必须告诉用户删除了哪些版本和文件，以及是否可恢复。

## 审核 GIF 清理

- 动作效果通过后，必须询问用户是否删除本次审核对应的 GIF 和 GIF 专用临时资源。
- 只有用户明确回复同意删除后，才能执行删除；通过动作或允许发布不等于允许删除 GIF。
- 删除前必须列出将删除的精确文件，只删除 `/Users/admin/Desktop/xiaoxiao-gif` 中与本次审核明确关联的内容。
- 不得借清理 GIF 的名义删除源码、原始素材、正式发布资源或其他动作的审核文件。
- 用户未同意删除时保留相关文件，不重复催促。

## Git 管理

- 本项目使用 Git 管理迭代。
- 保持使用 `master` 作为默认分支，不主动改为其他分支名。
- 用户明确完成验收后，自动提交本次已经验收的改动，不需要再次询问是否提交。
- 动作修改以用户明确回复“通过”“可以发布”等措辞作为验收完成；未明确通过的动作不得进入 commit。
- 每次 commit 的提交信息必须清楚概括本次实际改动点，不能只写“更新”“修改”等含糊内容。
- 每个 commit 只能包含本次已验收的相关文件；提交前必须检查 diff，不能夹带未验收、无关或临时文件。
- 尚未验收、仍在制作 GIF 或等待反馈时，不创建正式功能 commit。
- 除验收后自动提交本次改动外，未经用户明确要求，不创建其他 commit，不推送远端，不改写历史。
- 不使用会丢失用户改动的 Git 操作。需要回撤审核中的动作修改时，只回撤本次明确记录的相关文件。
